<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button
      class="navbar-toggler"
      type="button"
      data-toggle="collapse"
      data-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup" style="flex:1; justify-content: space-between">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
      </div>
      <% if (!user) { %>
        <form action="/login" method="POST">
          <label>Login: </label>
          <input
        class="border border-primary"
        type="text"
        name="username"
        placeholder="Username"
        style="width: 150px; margin: 1em; border-radius: 4px"
          />
        <button type="submit" class="btn btn-primary">Login</button>
        </form>
      <% } else if (user) { %>
        <div style="display:flex; align-items: center; gap:8px">
          <h5 style="margin-bottom: 0; font-weight: normal;" class ="text-center">Logged in as <%= user.email %></h5>
          <form action="/logout" method="POST">
          <button type="submit" class="btn btn-primary">Logout</button>
          </form>
        </div>
      <% } %>
    </div>
  </nav>
</header>
